{
    "build": {
        "symlink-install": true,
        "cmake-args": [
            "-G",
            "Ninja",
            "-DCMAKE_PREFIX_PATH=./colcon/Library;./install",
            "-DCATKIN_SKIP_TESTING=ON",
            "-DCMAKE_BUILD_TYPE=Release"],
        "event-handlers": ["console_cohesion+"],
        "parallel-workers" : 1,
        "merge-install": true
    }
}
