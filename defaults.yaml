{
    "build": {
        "symlink-install": true,
        "cmake-args": [
            "-G",
            "Ninja",
            "-DCMAKE_PREFIX_PATH=C:/Miniconda/envs/colcon/Library;c:/install/ros",
            "-DCATKIN_SKIP_TESTING=ON",
            "-DCMAKE_BUILD_TYPE=Release",
            "-DCMAKE_CXX_FLAGS=-DBOOST_ALL_DYN_LINK=1"],
        "event-handlers": ["console_cohesion+"],
        "install-base": "c:/install/ros",
        "parallel-workers" : 1,
        "merge-install": true,
        "packages-up-to": ["azure_kinect_ros_driver"]
    }
}
