{
    "build": {
        "symlink-install": true,
        "cmake-args": [
            "-G",
            "Ninja",
            "-DCMAKE_PREFIX_PATH=C:/Users/tzuhs/Miniconda3/envs/colcon/Library;c:/install/ros",
            "-DCATKIN_SKIP_TESTING=ON",
            "-DCMAKE_BUILD_TYPE=Release"],
        "event-handlers": ["console_cohesion+"],
        "install-base": "c:/install/ros",
        "parallel-workers" : 1,
        "merge-install": true,
        "packages-up-to": ["azure_kinect_ros_driver"]
    }
}
